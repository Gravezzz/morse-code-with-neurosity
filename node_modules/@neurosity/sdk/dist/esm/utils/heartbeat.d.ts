import { Observable } from "rxjs";
import { DeviceStatus } from "../types/status";
export declare function heartbeatAwareStatus(status$: Observable<DeviceStatus>): Observable<DeviceStatus>;
export declare function deviceHasLostHeartbeat(status: DeviceStatus, lastLocalHeartbeat: number): boolean;
